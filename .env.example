# Example App — Development Configuration
#
# HOST        Server bind address (default: 127.0.0.1)
# PORT        Server listen port (default: 9000)
# SQLITE_PATH Path to the SQLite database file (default: example.db)
# DATABASE_URL PostgreSQL connection string (default: host=localhost dbname=zzz_demo user=zzz password=zzz)

HOST=127.0.0.1
PORT=9000
SQLITE_PATH=example.db
DATABASE_URL=host=localhost dbname=zzz_demo user=zzz password=zzz
