# Example App — Environment Configuration
#
# These variables override the comptime defaults from config/dev.zig or config/prod.zig.
# Build with: zig build run              (uses config/dev.zig)
#             zig build run -Denv=prod   (uses config/prod.zig)
#
# HOST              Server bind address (dev: 127.0.0.1, prod: 0.0.0.0)
# PORT              Server listen port (dev: 9000, prod: 8080)
# SECRET_KEY_BASE   Secret for sessions/CSRF — MUST be set in production
# DATABASE_URL      SQLite path or postgres:// URL (dev: sqlite:example.db)
# PG_DATABASE_URL   PostgreSQL connection string
# POOL_SIZE         Database connection pool size (dev: 5, prod: 10)
# LOG_LEVEL         Logging level: debug, info, warn, err
# SHOW_ERROR_DETAILS  Show stack traces in error pages (dev: true, prod: false)

HOST=127.0.0.1
PORT=9000
SECRET_KEY_BASE=dev-secret-not-for-production
DATABASE_URL=sqlite:example.db
PG_DATABASE_URL=postgres://zzz:zzz@localhost:5432/zzz_demo
POOL_SIZE=5
LOG_LEVEL=debug
SHOW_ERROR_DETAILS=true
